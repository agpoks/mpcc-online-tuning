# --- Claude / AI assistant artifacts (never commit) ---
.claude/
CLAUDE.md
.claude.json
.mcp.json

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ipynb_checkpoints/

# --- run artifacts ---
# Per-experiment scratch only. Figures are NOT excluded: a docs build cannot
# run IPOPT, so every figure has to be in the tree, and a global *.png rule
# drops new ones silently.
runs/
c_generated_code/
acados_ocp*.json

# --- sphinx build output ---
docs/_build/
