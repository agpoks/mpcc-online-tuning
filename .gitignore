# --- Claude / AI assistant artifacts (never commit) ---
.claude/
CLAUDE.md
.claude.json
.mcp.json

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ipynb_checkpoints/

# --- run artifacts ---
runs/
*.png
# ...except the docs figures, which are committed: a docs build cannot run
# IPOPT, so the figures have to be in the tree.
!docs/source/_static/plots/*.png
c_generated_code/
acados_ocp*.json

# --- sphinx build output ---
docs/_build/
